# Пусто → axios baseURL = origin; пути уже содержат /api/v1/... (dev-прокси /api → бэкенд).
VITE_API_URL=
VITE_APP_VERSION=0.1.0
VITE_SMARTCAPTCHA_CLIENT_KEY=
# false → реальный бэкенд; true → встроенные axios-моки (автономный фронт).
VITE_API_MOCKS_ENABLED=false
API_PROXY_TARGET=http://localhost:8085
